# API base URL for backend requests.
# In dev, defaults to http://localhost:3000 when running SAM local.
# For production, set to your API Gateway URL (e.g. https://xxx.execute-api.eu-central-1.amazonaws.com/Prod).
# VITE_API_BASE_URL=
